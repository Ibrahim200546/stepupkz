# 🔌 Информация о портах

## Текущая конфигурация

**Dev сервер работает на:** `http://localhost:8081`

**Почему не 8080?**
- Порт 8080 занят другим процессом (PID: 8576)
- Vite автоматически переключился на следующий доступный порт (8081)

---

## ✅ Решение: Использовать порт 8081

**Преимущества:**
- ✅ Не нужно останавливать другое приложение
- ✅ Vite автоматически выбрал свободный порт
- ✅ Всё работает без дополнительных действий

**Обновленные URL:**
```
Local:   http://localhost:8081
Admin:   http://localhost:8081/admin
Account: http://localhost:8081/account
Auth:    http://localhost:8081/auth
```

---

## 🛠️ Опция 1: Оставить порт 8081 (рекомендуется)

Просто используйте `http://localhost:8081` везде.

**Не нужно ничего менять!**

---

## 🛠️ Опция 2: Освободить порт 8080

Если действительно нужен порт 8080:

### Шаг 1: Найти процесс

```bash
netstat -ano | findstr :8080
# Результат: PID 8576
```

### Шаг 2: Узнать что это за процесс

```bash
tasklist /FI "PID eq 8576"
```

### Шаг 3: Остановить процесс

**Если это ваше приложение:**
```bash
taskkill /PID 8576 /F
```

⚠️ **ВНИМАНИЕ:** Убедитесь что это не критичный системный процесс!

### Шаг 4: Перезапустить dev сервер

```bash
npm run dev
```

Теперь должен запуститься на порту 8080.

---

## 🛠️ Опция 3: Изменить порт в конфиге

Если хотите конкретный порт (например, 3000):

### Файл: `vite.config.ts`

```typescript
export default defineConfig(({ mode }) => ({
  server: {
    host: "::",
    port: 3000, // ← Ваш желаемый порт
  },
  // ...
}));
```

### Перезапустить:

```bash
npm run dev
```

---

## 📝 Обновленная документация

Все гайды обновлены для порта **8081**:

- ✅ `README.md`
- ✅ `ADMIN_PANEL_GUIDE.md`
- ✅ `ADMIN_PASSWORD_SETUP.md`
- ✅ `SUPABASE_SETUP_GUIDE.md`

**Просто используйте `http://localhost:8081` во всех примерах!**

---

## 🚀 Быстрый старт

1. **Запустить dev сервер:**
   ```bash
   npm run dev
   ```

2. **Открыть в браузере:**
   ```
   http://localhost:8081
   ```

3. **Админ панель:**
   ```
   http://localhost:8081/admin
   ```

Готово! 🎉

---

## 🆘 Частые вопросы

### Q: Почему Vite выбрал 8081 вместо 8080?

**A:** В `vite.config.ts` указан порт 8080, но он занят. Vite автоматически ищет следующий свободный порт.

### Q: Можно ли вернуть порт 8080?

**A:** Да, остановите процесс который его занимает (PID 8576).

### Q: Как узнать какой порт используется?

**A:** При запуске `npm run dev` Vite показывает:
```
➜  Local:   http://localhost:8081/
```

### Q: Влияет ли порт на production?

**A:** Нет! В production используется порт 80/443 (HTTP/HTTPS). Dev порт только для разработки.

---

**Статус:** ✅ Всё работает на порту 8081  
**Дата:** 11 ноября 2025

Made with ❤️ in Kazakhstan
