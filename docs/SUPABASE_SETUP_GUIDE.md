# 🚀 Полное руководство по настройке Supabase для StepUp Shoes

**Проблема:** Проект Supabase не существует или был удален  
**Решение:** Создать новый проект и настроить с нуля

---

## 📋 Шаг 1: Создание проекта Supabase

### 1.1. Зарегистрируйтесь/Войдите в Supabase

1. Откройте https://supabase.com
2. Нажмите **"Start your project"** или **"Sign In"**
3. Войдите через GitHub, Google или Email

### 1.2. Создайте новый проект

1. После входа нажмите **"New Project"**
2. Заполните форму:
   - **Name:** `stepup-shoes` (или любое имя)
   - **Database Password:** Создайте надежный пароль (СОХРАНИТЕ ЕГО!)
   - **Region:** Выберите ближайший регион (например, `West EU (London)` или `Central EU (Frankfurt)`)
   - **Pricing Plan:** `Free` (достаточно для разработки)

3. Нажмите **"Create new project"**

⏳ Подождите 2-3 минуты пока проект создается...

### 1.3. Получите данные для подключения

После создания проекта:

1. Перейдите в **Settings** (⚙️ иконка слева внизу)
2. **API** раздел
3. Скопируйте:
   - **Project URL** (например: `https://abcdefghijk.supabase.co`)
   - **anon public key** (длинный JWT токен начинающийся с `eyJ...`)

---

## 📝 Шаг 2: Обновление .env файла

### 2.1. Откройте файл `.env` в корне проекта

Если файла нет, создайте его:

```bash
# В корне проекта (там где package.json)
copy .env.example .env
```

### 2.2. Вставьте ваши данные

Откройте `.env` и замените значения:

```env
# Supabase Configuration
VITE_SUPABASE_URL=https://ВАШИХ-ПРОЕКТ-ID.supabase.co
VITE_SUPABASE_ANON_KEY=ВАШ-ANON-KEY-ЗДЕСЬ

# OpenAI Configuration (необязательно, пока можно оставить пустым)
VITE_OPENAI_API_KEY=

# Analytics (необязательно)
VITE_GA_TRACKING_ID=
VITE_SENTRY_DSN=

# Environment
NODE_ENV=development
```

**Пример:**
```env
VITE_SUPABASE_URL=https://xyzabcdefgh.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSI...
```

### 2.3. Проверьте файл

❌ **НЕ КОММИТЬТЕ `.env` в git!**  
✅ Убедитесь что `.env` в `.gitignore`

---

## 🗄️ Шаг 3: Применение миграций БД

### 3.1. Вариант A: Через SQL Editor (РЕКОМЕНДУЕТСЯ для первого раза)

1. **Откройте Supabase Dashboard** → ваш проект
2. **SQL Editor** (иконка 📝 слева)
3. **Применяйте миграции по порядку:**

#### 3.1.1. Основная схема (основные таблицы)

Скопируйте содержимое файла:
```
supabase/migrations/20251030104924_29a3c44d-4624-475e-9aa9-b21be855ce6a.sql
```

Вставьте в SQL Editor → **RUN**

✅ Должны создаться таблицы: `profiles`, `user_roles`, `products`, `categories`, `brands`, `carts`, `orders` и др.

#### 3.1.2. Остальные миграции по порядку

```
supabase/migrations/20251031053840_d9329319-4a6f-4994-b8f5-30aca6aaae2f.sql
supabase/migrations/20251031053904_33282c8e-80fa-4dca-9a0e-cd60f5e0cab4.sql
supabase/migrations/20251103141513_cc902259-c8cd-439d-b310-981122c12ae7.sql
supabase/migrations/20251104145823_a7933b0a-94f0-47c0-b154-d6e47219f20f.sql
supabase/migrations/20251105122231_8e81507e-9a49-402e-90d7-d06bef01a93d.sql
supabase/migrations/20251111120000_chat_storage.sql
supabase/migrations/20251111120001_vendor_stats.sql
supabase/migrations/20251111130000_fix_chat_rls_recursion.sql
```

Копируйте содержимое каждого файла → Вставляйте в SQL Editor → **RUN**

⚠️ **ВАЖНО:** Если появляются ошибки типа "already exists" - это нормально, продолжайте дальше.

### 3.2. Вариант B: Через Supabase CLI (если установлен)

```bash
# Установить Supabase CLI (если еще не установлен)
npm install -g supabase

# Войти в аккаунт
supabase login

# Связать проект (используйте Project Reference ID из Dashboard)
supabase link --project-ref ВАШ-PROJECT-ID

# Применить все миграции
supabase db push
```

### 3.3. Проверка миграций

Выполните в SQL Editor:

```sql
-- Проверить созданные таблицы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

Должны быть таблицы:
- ✅ `profiles`
- ✅ `user_roles`
- ✅ `products`, `product_variants`, `product_images`
- ✅ `categories`, `brands`
- ✅ `carts`, `cart_items`
- ✅ `orders`, `order_items`
- ✅ `chats`, `chat_members`
- ✅ `messages`, `message_reads`, `message_reports`
- ✅ `vendors`, `vendor_products`
- ✅ `user_presence`

---

## 📦 Шаг 4: Настройка Storage для изображений

### 4.1. Создать bucket для загрузок чата

1. **Storage** (слева в меню)
2. **Create a new bucket**
3. Заполните:
   - **Name:** `chat-attachments`
   - **Public bucket:** ✅ **Включить** (галочка)
   - **File size limit:** `10` MB
   - **Allowed MIME types:** `image/*`

4. **Create bucket**

### 4.2. Создать bucket для продуктов (опционально)

Повторите для других buckets если нужно:
- `product-images` - изображения товаров
- `user-avatars` - аватары пользователей
- `vendor-logos` - логотипы продавцов

### 4.3. Проверить RLS policies для Storage

Миграция `20251111120000_chat_storage.sql` должна была создать policies автоматически.

Проверить в SQL Editor:

```sql
SELECT * FROM storage.buckets WHERE id = 'chat-attachments';
```

Если bucket не создался, выполните вручную:

```sql
INSERT INTO storage.buckets (id, name, public)
VALUES ('chat-attachments', 'chat-attachments', true)
ON CONFLICT (id) DO NOTHING;
```

---

## 👤 Шаг 5: Настройка аутентификации

### 5.1. Включить Email Provider

1. **Authentication** → **Providers**
2. **Email** - должен быть включен по умолчанию
3. Настройки:
   - ✅ **Enable Email provider**
   - ✅ **Confirm email** - можно отключить для разработки
   - **Site URL:** `http://localhost:5173` (для dev)

### 5.2. Настроить URL Redirects (для production позже)

1. **Authentication** → **URL Configuration**
2. **Redirect URLs:**
   - `http://localhost:5173/**` (для разработки)
   - `https://ваш-домен.com/**` (для production)

---

## 🧪 Шаг 6: Тестирование подключения

### 6.1. Перезапустить dev сервер

```bash
# Остановить текущий сервер (Ctrl+C)
# Запустить заново
npm run dev
```

### 6.2. Проверить подключение в браузере

Откройте консоль браузера (F12) на странице http://localhost:5173

```javascript
// Проверить клиент Supabase
console.log('Supabase URL:', import.meta.env.VITE_SUPABASE_URL);

// Проверить подключение
const { data, error } = await supabase
  .from('profiles')
  .select('count')
  .limit(1);

console.log('Connection test:', data, error);
// Если работает, вернет: data: [...], error: null
```

### 6.3. Зарегистрировать тестового пользователя

1. Откройте http://localhost:5173/auth
2. Регистрация:
   - Email: `test@example.com`
   - Password: `test123456`
   - First Name: `Test`
   - Last Name: `User`
   - Nickname: `testuser`

3. Нажмите **"Зарегистрироваться"**

✅ Если успешно - вас перенаправит на главную страницу

### 6.4. Проверить в БД

В SQL Editor:

```sql
-- Проверить созданного пользователя
SELECT * FROM auth.users LIMIT 1;

-- Проверить профиль
SELECT * FROM public.profiles LIMIT 1;

-- Проверить роль
SELECT * FROM public.user_roles LIMIT 1;
```

---

## 👨‍💼 Шаг 7: Назначение админа

### 7.1. Получить User ID

В SQL Editor:

```sql
-- Найти вашего пользователя
SELECT id, email FROM auth.users;
```

Скопируйте `id` (например: `123e4567-e89b-12d3-a456-426614174000`)

### 7.2. Назначить роль admin

```sql
-- Вставьте свой user_id вместо '...'
INSERT INTO public.user_roles (user_id, role)
VALUES ('ВАШ-USER-ID', 'admin')
ON CONFLICT (user_id, role) DO NOTHING;
```

### 7.3. Проверить доступ к админ панели

1. Откройте http://localhost:5173/admin
2. ✅ Должны увидеть админ панель (Users, Products, Orders, Chats)

---

## 📊 Шаг 8: Добавление тестовых данных

### 8.1. Создать категории

```sql
INSERT INTO public.categories (name, slug) VALUES
('Мужская обувь', 'men'),
('Женская обувь', 'women'),
('Детская обувь', 'kids'),
('Спортивная обувь', 'sports')
ON CONFLICT DO NOTHING;
```

### 8.2. Создать бренды

```sql
INSERT INTO public.brands (name, slug) VALUES
('Nike', 'nike'),
('Adidas', 'adidas'),
('Puma', 'puma'),
('Reebok', 'reebok'),
('New Balance', 'new-balance')
ON CONFLICT DO NOTHING;
```

### 8.3. Создать тестовый товар

```sql
-- Получить ID категории и бренда
SELECT id FROM public.categories WHERE slug = 'men' LIMIT 1; -- Запомните ID
SELECT id FROM public.brands WHERE slug = 'nike' LIMIT 1; -- Запомните ID

-- Вставить товар (замените UUID на реальные из запросов выше)
INSERT INTO public.products (
  name, 
  description, 
  price, 
  old_price,
  category_id, 
  brand_id,
  is_active,
  is_featured
) VALUES (
  'Nike Air Max 90',
  'Классические кроссовки Nike Air Max 90',
  25000,
  30000,
  'CATEGORY-UUID-HERE',
  'BRAND-UUID-HERE',
  true,
  true
);
```

---

## 🧪 Шаг 9: Финальное тестирование

### 9.1. Тест каталога

1. Откройте http://localhost:5173/catalog
2. ✅ Должны увидеть созданный товар

### 9.2. Тест чата

1. Зарегистрируйте второго пользователя (в инкогнито режиме)
2. Откройте http://localhost:5173/chat
3. Создайте чат между двумя пользователями
4. Отправьте сообщение
5. ✅ Должно отправиться без ошибки "infinite recursion"

### 9.3. Тест корзины

1. Добавьте товар в корзину
2. Перейдите в корзину
3. Оформите заказ
4. ✅ Заказ должен создаться

---

## ✅ Checklist финальной проверки

- [ ] Supabase проект создан и активен
- [ ] `.env` файл обновлен с новыми данными
- [ ] Все миграции применены (8 файлов)
- [ ] Storage bucket `chat-attachments` создан
- [ ] Тестовый пользователь зарегистрирован
- [ ] Админ роль назначена
- [ ] Тестовые данные (категории, бренды, товар) добавлены
- [ ] Каталог отображает товары
- [ ] Регистрация/авторизация работает
- [ ] Чат работает без ошибок
- [ ] Корзина и заказы работают

---

## 🆘 Частые проблемы

### Проблема: "Invalid API key"

**Решение:**
- Проверьте что `VITE_SUPABASE_ANON_KEY` правильно скопирован из Dashboard
- Перезапустите dev сервер: `npm run dev`

### Проблема: "relation does not exist"

**Решение:**
- Не все миграции применены
- Выполните миграции заново по порядку

### Проблема: "Row Level Security policy violation"

**Решение:**
- Убедитесь что последняя миграция применена: `20251111130000_fix_chat_rls_recursion.sql`

### Проблема: "Bucket not found"

**Решение:**
- Создайте bucket `chat-attachments` вручную в Storage

---

## 📞 Что делать дальше?

После настройки:

1. ✅ **Протестируйте весь функционал** по `CHAT_TESTING_GUIDE.md`
2. ✅ **Добавьте больше товаров** через админ панель
3. ✅ **Настройте production** когда будете готовы к деплою
4. ✅ **Включите email confirmation** в настройках Auth

---

## 🔗 Полезные ссылки

- 📚 [Supabase Documentation](https://supabase.com/docs)
- 🎓 [Supabase Auth Guide](https://supabase.com/docs/guides/auth)
- 💾 [Supabase Storage Guide](https://supabase.com/docs/guides/storage)
- 🔒 [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

---

**Время на настройку:** ~20-30 минут  
**Сложность:** Средняя  
**Результат:** Полностью настроенный проект с БД!

---

## 📝 После завершения настройки

Напишите мне:
- ✅ "Supabase настроен, все работает"
- Или опишите на каком шаге возникла проблема

Удачи с настройкой! 🚀
