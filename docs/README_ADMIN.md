# 📊 Админ Панель - StepUp Shoes

## ✅ Что работает СЕЙЧАС

### Старая Админ Панель (Рекомендуется)
**URL:** http://localhost:8080/admin-old

**Функции:**
- ✅ Dashboard с статистикой
- ✅ User Management - управление пользователями
- ✅ Product Management - управление товарами
- ✅ Order Management - управление заказами
- ✅ Vendor Management - управление продавцами
- ✅ Chat Management - мониторинг чатов
- ✅ Reports - отчеты и аналитика

**Технологии:**
- React + TypeScript
- Supabase для данных
- Shadcn/UI компоненты
- Real-time updates

### Тестовая Страница
**URL:** http://localhost:8080/admin-test

**Показывает:**
- Профиль пользователя
- Статус админа
- Доступ к данным
- Статистику системы
- Роли пользователя

## ⚠️ Slash Admin - В разработке

**URL:** http://localhost:8080/admin

**Статус:** Временно недоступен

**Причина:** Конфликт путей импорта
- Slash Admin использует `@/components/...`
- Эти пути должны указывать на `src/admin-panel/`
- Но сейчас указывают на `src/`

**Placeholder:** Показывает информацию о проблеме и ссылки на рабочие админки

## 🚀 Быстрый старт

### 1. Настройте права админа в Supabase

```sql
-- В Supabase SQL Editor выполните:
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS is_admin BOOLEAN DEFAULT false;
UPDATE profiles SET is_admin = true WHERE email = 'ВАШ_EMAIL@example.com';
```

### 2. Откройте админку

```bash
# Запустите проект
npm run dev

# Откройте в браузере:
http://localhost:8080/admin-old
```

### 3. Авторизуйтесь

1. Войдите через `/auth` с вашим email
2. Перейдите на `/admin-old`
3. Вы должны увидеть dashboard

## 📁 Структура админок

```
src/
├── components/admin/          # Старая админка (РАБОТАЕТ ✅)
│   ├── Dashboard.tsx
│   ├── UsersManagement.tsx
│   ├── ProductsManagement.tsx
│   ├── OrdersManagement.tsx
│   ├── VendorsManagement.tsx
│   ├── ChatsManagement.tsx
│   ├── ReportsManagement.tsx
│   └── AdminLayout.tsx
│
├── admin-panel/              # Slash Admin (В разработке ⚠️)
│   ├── layouts/
│   ├── pages/
│   ├── components/
│   ├── api/
│   │   └── supabaseAdapter.ts
│   └── ...
│
└── pages/
    ├── Admin.tsx             # Старая админка
    ├── SlashAdmin.tsx        # Placeholder для Slash Admin
    └── AdminTest.tsx         # Тестирование доступа
```

## 🔐 Проверка прав доступа

Админ панель защищена двумя способами:

### 1. Проверка в базе данных
```tsx
// Проверяет is_admin в profiles
const { data: profile } = await supabase
  .from('profiles')
  .select('is_admin')
  .eq('id', user.id)
  .single();

if (!profile?.is_admin) {
  // Доступ запрещен
}
```

### 2. Проверка ролей
```tsx
// Проверяет роли в user_roles
const { data: roles } = await supabase
  .from('user_roles')
  .select('role')
  .eq('user_id', user.id);

const isAdmin = roles?.some(r => r.role === 'admin');
```

## 📊 Доступные функции

### Dashboard
- Статистика пользователей
- Статистика заказов
- Статистика товаров
- Статистика продавцов
- Графики продаж

### User Management
- Просмотр всех пользователей
- Поиск и фильтрация
- Изменение ролей
- Блокировка пользователей

### Product Management  
- Список всех товаров
- Создание товара
- Редактирование
- Удаление
- Управление категориями

### Order Management
- Просмотр заказов
- Изменение статуса
- Детали заказа
- История изменений

### Vendor Management
- Список продавцов
- Активация/деактивация
- Просмотр товаров продавца
- Статистика продавца

### Chat Management
- Мониторинг чатов
- Просмотр сообщений
- Статистика активности

## 🐛 Troubleshooting

### "Access Denied" при открытии админки

**Решение:**
1. Проверьте: `SELECT is_admin FROM profiles WHERE id = 'ваш-id'`
2. Если `false` → выполните: `UPDATE profiles SET is_admin = true WHERE id = 'ваш-id'`

### 500 Internal Server Error

**Решение:**
1. Проверьте RLS policies в Supabase
2. Запустите: `SQL/fix_admin_quick.sql`
3. Перезагрузите страницу

### Infinite Recursion Error

**Решение:**
Запустите в Supabase SQL Editor:
```sql
DROP POLICY IF EXISTS "Admins can view all profiles" ON profiles;
CREATE POLICY "Admins can view all profiles"
ON profiles FOR SELECT TO authenticated
USING (is_current_user_admin() = true);
```

## 📚 Документация

- `ADMIN_FIX_GUIDE.md` - Руководство по исправлению проблем
- `SQL/fix_admin_quick.sql` - Быстрая настройка SQL
- `SLASH_ADMIN_PATHS_FIX.md` - Проблема путей импорта
- `SLASH_ADMIN_INTEGRATION.md` - Интеграция Slash Admin
- `FINAL_SETUP_GUIDE.md` - Общая настройка

## 🎯 Рекомендации

### Для production:

1. **Используйте старую админку** (`/admin-old`)
   - Полностью функциональна
   - Интегрирована с Supabase
   - Протестирована

2. **Настройте RLS policies**
   - Запустите `SQL/fix_admin_quick.sql`
   - Проверьте права доступа

3. **Установите is_admin**
   - Для всех админов в БД
   - Проверьте через `/admin-test`

4. **Мониторьте доступ**
   - Регулярно проверяйте логи
   - Используйте `/admin-test` для диагностики

### Для разработки:

1. **Если нужен Slash Admin:**
   - См. `SLASH_ADMIN_PATHS_FIX.md`
   - Исправьте импорты вручную
   - Или используйте старую админку

2. **Добавление функций:**
   - Расширяйте компоненты в `src/components/admin/`
   - Используйте Supabase для данных
   - Следуйте существующим паттернам

## 📌 Quick Links

- **Админка (старая):** http://localhost:8080/admin-old
- **Slash Admin:** http://localhost:8080/admin (placeholder)
- **Тест доступа:** http://localhost:8080/admin-test
- **Главная:** http://localhost:8080/

## 💡 Tips

- **Console logs**: Проверяйте Console (F12) для debug
- **Network tab**: Мониторьте запросы к Supabase
- **Test page**: Используйте `/admin-test` для диагностики
- **SQL Editor**: Быстрые проверки в Supabase

## 🎉 Готово!

Старая админ панель полностью функциональна и готова к использованию!

**Начните здесь:**
1. Настройте `is_admin` в Supabase
2. Откройте http://localhost:8080/admin-old
3. Начните управлять системой!

**Удачи! 🚀**
