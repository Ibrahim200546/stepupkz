# 🤖 Настройка AI Chat с OpenAI GPT

## Что было создано:

1. ✅ **Supabase Edge Function** (`supabase/functions/ai-chat/index.ts`)
   - Безопасный proxy для OpenAI API
   - API ключ хранится на сервере (не на фронтенде!)
   - Поддержка истории разговора
   - Обработка ошибок

2. ✅ **AI Chat Widget** (`src/components/chat/AIChatWidget.tsx`)
   - Красивый floating чат в правом нижнем углу
   - Минимизация/развертывание
   - История сообщений
   - Loading состояния

3. ✅ **Интеграция в App.tsx**
   - Виджет доступен на всех страницах
   - Автоматически появляется справа внизу

---

## 🚀 Настройка (3 шага):

### Шаг 1: Добавить OpenAI ключ в .env

```env
# В файле .env
VITE_OPENAI_API_KEY=sk-proj-ваш-новый-ключ
```

⚠️ **Убедитесь что это НОВЫЙ ключ** (старый отозван!)

### Шаг 2: Deploy Edge Function в Supabase

**Вариант A: Через Supabase CLI**

```bash
# 1. Установить Supabase CLI (если еще нет)
npm install -g supabase

# 2. Войти в аккаунт
supabase login

# 3. Связать с проектом
supabase link --project-ref uoziiapuqunqbvevfzyu

# 4. Добавить OPENAI_API_KEY в Supabase секреты
supabase secrets set OPENAI_API_KEY=sk-proj-ваш-новый-ключ

# 5. Deploy функцию
supabase functions deploy ai-chat
```

**Вариант B: Через Supabase Dashboard (если CLI не работает)**

1. Откройте: https://supabase.com/dashboard/project/uoziiapuqunqbvevfzyu
2. **Edge Functions** → **Create Function**
3. Name: `ai-chat`
4. Скопируйте код из `supabase/functions/ai-chat/index.ts`
5. **Settings** → **Secrets** → Add:
   - Key: `OPENAI_API_KEY`
   - Value: `sk-proj-ваш-новый-ключ`
6. **Deploy**

### Шаг 3: Перезапустить dev сервер

```bash
npm run dev
```

---

## ✅ Проверка что работает:

1. **Откройте сайт:** http://localhost:8081
2. **В правом нижнем углу** должна быть кнопка с иконкой робота 🤖
3. **Нажмите на кнопку** → откроется чат
4. **Напишите:** "Привет"
5. **AI должен ответить** в течение 1-3 секунд

---

## 🎨 Функции AI помощника:

### Что умеет:

- ✅ Отвечает на вопросы о товарах
- ✅ Помогает с выбором обуви
- ✅ Консультирует по размерам
- ✅ Информация о доставке и оплате
- ✅ Помощь с возвратом/обменом
- ✅ Рекомендации товаров
- ✅ Поддержка 3 языков (RU/KZ/EN)

### Примеры вопросов:

```
"Какие у вас есть кроссовки Nike?"
"Как определить свой размер?"
"Сколько стоит доставка?"
"Можно ли вернуть товар?"
"Порекомендуй обувь для бега"
```

---

## 💰 Стоимость использования:

**Модель:** GPT-4o-mini (самая дешевая)

**Цены:**
- Input: $0.15 / 1M tokens
- Output: $0.60 / 1M tokens

**Примерные расходы:**
- 1 сообщение ≈ 500 tokens
- 1000 сообщений ≈ $0.50
- Очень дешево! 💰

**Лимиты:**
- 500 tokens на ответ (достаточно для консультации)
- История последних 5 сообщений

---

## 🛡️ Безопасность:

### ✅ Что сделано:

1. **API ключ НЕ на фронтенде**
   - Хранится в Supabase Secrets
   - Фронтенд не имеет доступа к ключу

2. **Edge Function как proxy**
   - Все запросы идут через Supabase
   - Ключ защищен

3. **Rate limiting** (встроенный в Supabase)
   - Автоматические лимиты
   - Защита от злоупотреблений

4. **История ограничена**
   - Только последние 5 сообщений
   - Экономия токенов

### ⚠️ Для production:

```typescript
// TODO: Добавить в Edge Function:

// 1. Аутентификация
const { data: { user } } = await supabaseClient.auth.getUser();
if (!user) throw new Error('Unauthorized');

// 2. Rate limiting per user
const userLimit = await checkUserRateLimit(user.id);
if (userLimit.exceeded) throw new Error('Too many requests');

// 3. Логирование
await logAIRequest(user.id, message, botMessage);
```

---

## 🎨 UI Features:

### Виджет:

- 🎨 Красивый дизайн (shadcn/ui)
- 📱 Адаптивный (работает на мобильных)
- 🔽 Минимизация/развертывание
- ❌ Закрытие кнопкой X
- 💬 История сообщений
- ⏱️ Временные метки
- 🔄 Loading индикатор (3 точки)
- ⌨️ Enter для отправки

### Позиция:

```css
position: fixed;
bottom: 24px;
right: 24px;
z-index: 50;
```

Не мешает другим элементам!

---

## 🔧 Настройка AI поведения:

### System Prompt (в Edge Function):

```typescript
const messages = [
  { 
    role: 'system', 
    content: `Ты — AI-помощник магазина StepUp Shoes...`
  },
  // ...
];
```

### Можно изменить:

1. **Личность AI**
   - Более формальная/неформальная
   - Добавить эмодзи
   - Изменить тон

2. **Знания о магазине**
   - Обновить информацию о доставке
   - Добавить акции
   - Описать новые услуги

3. **Языки**
   - Добавить другие языки
   - Установить приоритетный язык

### Пример настройки:

```typescript
content: `Ты — веселый и дружелюбный AI-помощник StepUp! 🎉

Используй больше эмодзи 😊👟✨
Будь креативным в рекомендациях!
Поддерживай позитивную атмосферу!

Акции:
- Скидка 20% на Nike до конца месяца! 🔥
- Бесплатная доставка при заказе от 30,000₸

...
`
```

---

## 🐛 Troubleshooting:

### Ошибка: "AI-помощник временно недоступен"

**Причины:**
1. OpenAI API ключ не настроен
2. Функция не задеплоена
3. Нет кредитов на OpenAI

**Решение:**
```bash
# Проверить секреты
supabase secrets list

# Проверить функцию
supabase functions list

# Посмотреть логи
supabase functions logs ai-chat
```

### Ошибка: "Превышен лимит запросов"

**Причина:** Rate limit OpenAI (60 requests/min)

**Решение:** Подождите минуту

### Ошибка: "Неверный API ключ"

**Причина:** Ключ не добавлен в Supabase secrets

**Решение:**
```bash
supabase secrets set OPENAI_API_KEY=sk-proj-новый-ключ
```

### Кнопка чата не появляется

**Решение:**
1. Проверить консоль браузера (F12)
2. Убедиться что `<AIChatWidget />` в App.tsx
3. Перезапустить dev сервер

---

## 📊 Мониторинг:

### OpenAI Dashboard:

```
https://platform.openai.com/usage
```

Смотрите:
- Количество запросов
- Стоимость
- Использованные токены

### Supabase Functions Logs:

```bash
supabase functions logs ai-chat --tail
```

Или в Dashboard:
```
Edge Functions → ai-chat → Logs
```

---

## 🎯 Улучшения (TODO):

### Сейчас:
- ✅ Базовый AI чат
- ✅ OpenAI GPT-4o-mini
- ✅ История 5 сообщений
- ✅ Безопасное хранение ключа

### В будущем:
- [ ] Аутентификация пользователей
- [ ] Rate limiting per user
- [ ] Сохранение истории в БД
- [ ] Кнопки быстрых вопросов
- [ ] Интеграция с каталогом (рекомендации товаров)
- [ ] Voice input (распознавание речи)
- [ ] Отправка изображений обуви

---

## ✅ Checklist готовности:

- [ ] OpenAI ключ создан и добавлен в .env
- [ ] Edge Function задеплоена в Supabase
- [ ] OPENAI_API_KEY добавлен в Supabase secrets
- [ ] Dev сервер перезапущен
- [ ] Кнопка чата видна на сайте
- [ ] Тестовое сообщение отправлено и получен ответ
- [ ] Проверен usage в OpenAI dashboard

---

**Статус:** ✅ Готов к использованию после deploy!  
**Модель:** GPT-4o-mini  
**Стоимость:** ~$0.0005 на сообщение  

Задеплойте Edge Function и AI помощник заработает! 🤖
