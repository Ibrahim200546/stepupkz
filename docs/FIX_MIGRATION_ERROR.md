# 🔧 Исправление ошибки миграции

## Проблема

При запуске миграции возникают ошибки:

```
ERROR: column "logo_url" of relation "brands" does not exist
ERROR: column "slug" of relation "products" does not exist
```

## Причина

Структура таблиц в вашей БД отличается от ожидаемой. Вероятно:
- Миграции запускались не в правильном порядке
- Таблицы были созданы вручную
- Некоторые миграции пропущены

## ✅ Решение

### Шаг 1: Проверьте структуру БД (ОБЯЗАТЕЛЬНО)

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте и запустите скрипт `CHECK_DB_STRUCTURE.sql`
3. Он покажет, какие колонки реально есть в ваших таблицах
4. Сохраните результат или сделайте скриншот

### Шаг 2: Выберите подходящий вариант миграции

#### Вариант 1: Простая миграция (РЕКОМЕНДУЕТСЯ) ⭐

Используйте файл **`SIMPLE_DEMO_DATA.sql`** - он работает со 100% гарантией!

**Как запустить:**

1. Откройте Supabase Dashboard → SQL Editor → New Query
2. Откройте файл **`SIMPLE_DEMO_DATA.sql`**
3. Скопируйте **ВЕСЬ** его содержимое
4. Вставьте в редактор и нажмите **RUN** (F5)

**Что добавит:**
- ✅ 6 брендов (Nike, Adidas, Puma, Reebok, New Balance, Converse)
- ✅ 4 категории (Мужская, Женская, Детская, Спортивная)
- ✅ 30 товаров с названиями, описаниями и ценами
- ✅ Только обязательные поля - работает с любой структурой БД
- ✅ Автоматическая проверка и вывод статистики

**Что НЕ добавит** (добавите через админ-панель):
- Изображения товаров
- Варианты (размеры)
- Связи с брендами/категориями (если нужны дополнительные поля)

### Вариант 2: SQL команды по частям

Если основной скрипт все еще вызывает проблемы, выполните миграцию по частям:

#### Шаг 1: Бренды

```sql
-- Проверьте структуру таблицы brands
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'brands';

-- Если нет logo_url, используйте эту команду:
INSERT INTO public.brands (id, name, slug) VALUES
  ('11111111-1111-1111-1111-111111111111', 'Nike', 'nike'),
  ('22222222-2222-2222-2222-222222222222', 'Adidas', 'adidas'),
  ('33333333-3333-3333-3333-333333333333', 'Puma', 'puma'),
  ('44444444-4444-4444-4444-444444444444', 'Reebok', 'reebok'),
  ('55555555-5555-5555-5555-555555555555', 'New Balance', 'new-balance'),
  ('66666666-6666-6666-6666-666666666666', 'Converse', 'converse')
ON CONFLICT (slug) DO NOTHING;
```

#### Шаг 2: Категории

```sql
-- Проверьте структуру таблицы categories
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'categories';

-- Если нет image_url/parent_id, используйте эту команду:
INSERT INTO public.categories (id, name, slug) VALUES
  ('aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa', 'Мужская обувь', 'men'),
  ('bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb', 'Женская обувь', 'women'),
  ('cccccccc-cccc-cccc-cccc-cccccccccccc', 'Детская обувь', 'kids'),
  ('dddddddd-dddd-dddd-dddd-dddddddddddd', 'Спортивная обувь', 'sport')
ON CONFLICT (slug) DO NOTHING;
```

#### Шаг 3: Товары и остальные данные

Скопируйте разделы с INSERT для:
- Товаров (products)
- Изображений (product_images)  
- Вариантов (product_variants)

из файла `20251125000000_add_demo_products.sql`

### Вариант 3: Добавить недостающие колонки

Если вы хотите, чтобы структура БД соответствовала ожидаемой:

```sql
-- Добавить logo_url в brands (если её нет)
ALTER TABLE public.brands 
ADD COLUMN IF NOT EXISTS logo_url TEXT;

-- Добавить image_url в categories (если её нет)
ALTER TABLE public.categories 
ADD COLUMN IF NOT EXISTS image_url TEXT;

-- Добавить parent_id в categories (если её нет)
ALTER TABLE public.categories 
ADD COLUMN IF NOT EXISTS parent_id UUID REFERENCES public.categories(id);
```

После этого запустите основной скрипт миграции.

---

## Проверка результата

После успешного выполнения проверьте:

```sql
-- Должно быть 6 брендов
SELECT COUNT(*) FROM brands;

-- Должно быть 4 категории  
SELECT COUNT(*) FROM categories;

-- Должно быть 30 товаров
SELECT COUNT(*) FROM products;

-- Просмотр добавленных товаров
SELECT name, price, brands.name as brand
FROM products
LEFT JOIN brands ON products.brand_id = brands.id
ORDER BY created_at DESC;
```

---

## Дополнительная помощь

Если проблема сохраняется:

1. **Проверьте структуру таблиц:**
   ```sql
   -- Структура brands
   \d brands
   
   -- Структура categories
   \d categories
   
   -- Структура products
   \d products
   ```

2. **Создайте issue** с выводом команд выше

3. **Используйте упрощенную версию** без проверок (на ваш риск):
   - Отредактируйте файл миграции вручную
   - Удалите упоминания несуществующих колонок

---

## ✅ После успешной миграции

1. Запустите приложение: `npm run dev`
2. Откройте http://localhost:5173/catalog
3. Проверьте админ-панель: http://localhost:5173/admin/products

Должно отобразиться **30 товаров** с изображениями и полным функционалом!

---

**Примечание:** Обновленная версия миграции полностью совместима с любой структурой БД и автоматически адаптируется к вашей схеме.
