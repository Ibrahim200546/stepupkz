# 🧪 Тестирование запуска Slash Admin

## ✅ Исправления выполнены:

### 1. PostCSS и Tailwind
- ✅ Создан отдельный `postcss.config.js` для admin-panel
- ✅ Отключен Tailwind (используется Ant Design)
- ✅ Удалены `@layer` и `@theme` директивы

### 2. Импорты
- ✅ Добавлен alias `#` → `src/admin-panel` для `#/enum`
- ✅ Исправлен импорт `package.json` (закомментирован)
- ✅ Отключена Vercel Analytics
- ✅ Отключен react-scan

### 3. Vite config
- ✅ Кастомный PostCSS config в `vite.config.admin.ts`
- ✅ Правильные alias: `@`, `~`, `#`
- ✅ Root: `./src/admin-panel`

## 🚀 Команды для запуска:

### Шаг 1: Очистить кеш
```bash
powershell -Command "Remove-Item -Recurse -Force node_modules\.vite"
```

### Шаг 2: Запустить admin
```bash
npm run dev:admin
```

**Ожидаемый результат:**
```
VITE v5.4.21  ready in xxx ms

➜  Local:   http://localhost:3001/
➜  Network: use --host to expose
```

### Шаг 3: Запустить основное приложение (в другом терминале)
```bash
npm run dev
```

### Шаг 4: Открыть в браузере
```
http://localhost:8080/admin-iframe
```

## 🔍 Проверка:

После запуска `npm run dev:admin` проверьте:

- [ ] Нет ошибок PostCSS
- [ ] Нет ошибок `#/enum`
- [ ] Нет ошибок `package.json`
- [ ] Нет ошибок `@vercel/analytics`
- [ ] Сервер запущен на порту 3001
- [ ] Можно открыть http://localhost:3001/

## 📊 Возможные проблемы:

### Если видите:
```
[vite] Pre-transform error: Failed to resolve import "#/enum"
```

**Решение:** Проверьте что в `vite.config.admin.ts` есть:
```typescript
alias: {
  "#": path.resolve(__dirname, "./src/admin-panel"),
}
```

### Если видите:
```
[postcss] Unknown word "use strict"
```

**Решение:** Убедитесь что `global.css` не импортирует Tailwind.

### Если видите:
```
Cannot find module '@vercel/analytics'
```

**Решение:** Проверьте что в `App.tsx` импорт закомментирован.

## ✅ Следующие шаги после успешного запуска:

1. Проверить что admin-panel загружается на http://localhost:3001/
2. Запустить основное приложение `npm run dev`
3. Открыть iframe: http://localhost:8080/admin-iframe
4. Проверить авторизацию и доступ

## 📚 Документация:

- **TEST_ADMIN_LAUNCH.md** (этот файл)
- **FIX_ADMIN_PANEL.md** - исправления PostCSS
- **QUICK_START_SLASH_ADMIN.md** - быстрый старт
- **SLASH_ADMIN_SEPARATE_BUILD.md** - полная документация
