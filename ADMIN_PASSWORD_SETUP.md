# 🔐 Настройка пароля админ панели

**Версия:** 1.0.0  
**Дата:** 11 ноября 2025

---

## 🎯 Обзор системы безопасности

Админ панель теперь защищена **двухфакторной системой**:

1. **Роль admin/manager** в БД (`user_roles`)
2. **16-значный пароль** для дополнительной защиты

---

## 🔑 Установка пароля

### Шаг 1: Открыть файл конфигурации

**Файл:** `src/components/admin/AdminPasswordDialog.tsx`

```typescript
// Строка 17
const ADMIN_PANEL_PASSWORD = "StepUp2025Admin!"; // ← Измените на свой!
```

### Шаг 2: Создать надежный пароль

**Требования:**
- Минимум 16 символов
- Содержит заглавные и строчные буквы
- Содержит цифры
- Содержит специальные символы

**Примеры надежных паролей:**
```
MyS3cur3P@ssw0rd!
StepUpKZ#2025$Pro
Admin!SecureKZ99
P@ssw0rd_Str0ng16
```

**Генератор:**
```bash
# В PowerShell
-join ((48..57) + (65..90) + (97..122) + 33,35,36,37,64 | Get-Random -Count 16 | ForEach-Object {[char]$_})
```

### Шаг 3: Сохранить пароль

⚠️ **ВАЖНО:**
- **НЕ КОММИТЬТЕ** файл с реальным паролем в git
- Сохраните пароль в **безопасном месте** (менеджер паролей)
- Не передавайте пароль по незащищенным каналам

### Шаг 4: Перезапустить проект

```bash
npm run build
npm run dev
```

---

## 🚀 Как это работает

### Сценарий входа в админку:

```
1. Пользователь открывает /account
   ↓
2. Если роль = admin/manager → видит кнопку "Админ панель"
   ↓
3. Нажимает кнопку → открывается модальное окно
   ↓
4. Вводит 16-значный пароль
   ↓
5. Если пароль верный:
   - Сохраняется в sessionStorage
   - Перенаправление на /admin
   - Доступ действует 8 часов
   ↓
6. Если пароль неверный:
   - Попытка засчитывается (макс 3)
   - После 3 попыток → блокировка на 5 минут
```

### Безопасность:

**✅ Защита от перебора:**
- Макс 3 попытки
- Блокировка на 5 минут после 3 неудачных попыток
- Счетчик попыток сбрасывается при закрытии окна

**✅ Срок действия:**
- Верификация действует 8 часов
- После истечения требуется повторный ввод
- Хранится в `sessionStorage` (не survives browser restart)

**✅ Двухфакторная защита:**
- Даже если кто-то узнал пароль, нужна роль admin в БД
- Даже если есть роль admin, нужен пароль

---

## 📋 Использование

### Для админа:

1. **Войти в аккаунт** (http://localhost:8080/auth)
2. **Перейти в профиль** (http://localhost:8080/account)
3. **Увидеть карточку "Админ панель"** (если есть роль)
4. **Нажать "Открыть админ панель"**
5. **Ввести пароль**
6. **Получить доступ**

### Если нет кнопки:

**Причина:** Нет роли `admin` или `manager` в БД

**Решение:**
```sql
-- Supabase Dashboard → SQL Editor

-- Проверить роли
SELECT * FROM user_roles WHERE user_id = 'ваш-id';

-- Назначить роль admin
INSERT INTO user_roles (user_id, role)
VALUES ('ваш-id', 'admin');
```

---

## 🔒 Безопасность: За и против

### ✅ Преимущества 16-значного пароля:

1. **Дополнительный слой защиты**
   - Даже если БД скомпрометирована, пароль защищает
   
2. **Защита от инсайдеров**
   - Разработчик с доступом к БД не может войти без пароля
   
3. **Централизованный контроль**
   - Один пароль для всех админов
   - Легко сменить при необходимости
   
4. **Защита от session hijacking**
   - Кража session токена не даст доступа к админке

5. **Удобно для команды**
   - Не нужно каждому назначать роль
   - Можно дать временный доступ

### ⚠️ Потенциальные риски:

1. **Single point of failure**
   - Если пароль утечет, все админы под угрозой
   - **Решение:** Регулярно менять пароль

2. **Жесткая кодировка**
   - Пароль в коде (но не в git!)
   - **Решение:** Использовать переменные окружения

3. **Сложность для пользователя**
   - Нужно помнить/хранить пароль
   - **Решение:** Менеджер паролей

### 🛡️ Рекомендации по безопасности:

**Обязательно:**
- ✅ Использовать сложный 16+ символьный пароль
- ✅ Хранить пароль в менеджере паролей (1Password, Bitwarden)
- ✅ Менять пароль раз в 3 месяца
- ✅ Не передавать пароль по email/SMS
- ✅ Не сохранять пароль в браузере

**Дополнительно:**
- ⭐ Использовать HTTPS в production
- ⭐ Включить 2FA для аккаунта Supabase
- ⭐ Мониторить логи доступа
- ⭐ Использовать VPN для удаленного доступа
- ⭐ Регулярно проверять список админов

---

## 🔧 Настройка без пароля (не рекомендуется)

Если хотите убрать пароль (ТОЛЬКО для dev):

### Вариант 1: Отключить полностью

**Файл:** `src/pages/Admin.tsx`

```typescript
// Закомментировать проверку пароля
// if (!passwordVerified) {
//   return <Navigate to="/account" replace />;
// }
```

### Вариант 2: Автоматическая верификация

**Файл:** `src/pages/Admin.tsx`

```typescript
useEffect(() => {
  // Автоматически верифицировать в dev режиме
  if (import.meta.env.DEV) {
    sessionStorage.setItem('admin_verified', 'true');
    sessionStorage.setItem('admin_verified_at', Date.now().toString());
    setPasswordVerified(true);
  }
}, []);
```

⚠️ **НЕ ИСПОЛЬЗУЙТЕ в production!**

---

## 🔄 Изменение пароля

### Шаг 1: Изменить в коде

**Файл:** `src/components/admin/AdminPasswordDialog.tsx`

```typescript
// Старый пароль
const ADMIN_PANEL_PASSWORD = "StepUp2025Admin!";

// Новый пароль
const ADMIN_PANEL_PASSWORD = "NewSecureP@ss16!";
```

### Шаг 2: Очистить sessionStorage

Все текущие сессии станут невалидными.

```bash
# Rebuild проекта
npm run build
```

### Шаг 3: Уведомить админов

Отправить новый пароль всем админам через безопасный канал.

---

## 📊 Мониторинг доступа

### Проверить активные сессии:

```javascript
// В консоли браузера (F12)
const verified = sessionStorage.getItem('admin_verified');
const verifiedAt = sessionStorage.getItem('admin_verified_at');

if (verified) {
  const time = new Date(parseInt(verifiedAt));
  console.log('Верифицирован:', time.toLocaleString());
  
  const expiresIn = 8 * 60 * 60 * 1000 - (Date.now() - parseInt(verifiedAt));
  console.log('Истекает через:', Math.round(expiresIn / 60000), 'минут');
}
```

### Проверить блокировки:

```javascript
const blockedUntil = sessionStorage.getItem('admin_blocked_until');
if (blockedUntil) {
  const time = new Date(parseInt(blockedUntil));
  console.log('Заблокирован до:', time.toLocaleString());
}
```

### Сбросить блокировку (admin):

```javascript
sessionStorage.removeItem('admin_blocked_until');
```

---

## 🧪 Тестирование

### Тест 1: Успешный вход

```
1. Открыть /account
2. Нажать "Открыть админ панель"
3. Ввести правильный пароль
4. ✅ Перенаправление на /admin
```

### Тест 2: Неверный пароль

```
1. Открыть /account
2. Нажать "Открыть админ панель"
3. Ввести неверный пароль
4. ✅ Сообщение "Неверный пароль. Осталось 2 попытки"
```

### Тест 3: Блокировка

```
1. Ввести неверный пароль 3 раза
2. ✅ Сообщение "Доступ заблокирован на 5 минут"
3. ✅ Окно закрывается
4. ✅ Повторное открытие показывает блокировку
```

### Тест 4: Срок действия

```
1. Войти с правильным паролем
2. Открыть /admin - работает
3. В sessionStorage изменить admin_verified_at на старую дату
4. Обновить страницу /admin
5. ✅ Перенаправление на /account
```

---

## 🆘 Частые проблемы

### Проблема 1: Забыл пароль

**Решение:**
1. Откройте `src/components/admin/AdminPasswordDialog.tsx`
2. Посмотрите значение `ADMIN_PANEL_PASSWORD`
3. Или установите новый пароль

### Проблема 2: Блокировка не снимается

**Решение:**
```javascript
// В консоли браузера
sessionStorage.clear();
```

### Проблема 3: Кнопка админ панели не появляется

**Причина:** Нет роли admin

**Решение:**
```sql
SELECT * FROM user_roles WHERE user_id = auth.uid();
INSERT INTO user_roles (user_id, role) VALUES (auth.uid(), 'admin');
```

### Проблема 4: После ввода пароля редирект на главную

**Причина:** Верификация не сохраняется

**Решение:**
- Проверить консоль браузера на ошибки
- Проверить что sessionStorage работает:
```javascript
sessionStorage.setItem('test', 'value');
console.log(sessionStorage.getItem('test')); // 'value'
```

---

## 🔐 Альтернативные варианты безопасности

### Вариант 1: Environment Variable (рекомендуется для production)

**Файл:** `.env`
```env
VITE_ADMIN_PASSWORD=YourSecurePassword16!
```

**Файл:** `AdminPasswordDialog.tsx`
```typescript
const ADMIN_PANEL_PASSWORD = import.meta.env.VITE_ADMIN_PASSWORD || "fallback";
```

**Преимущества:**
- ✅ Пароль не в git
- ✅ Легко менять per environment
- ✅ Безопаснее

**Недостатки:**
- ⚠️ Нужно настраивать на каждом сервере
- ⚠️ Пароль виден в build

### Вариант 2: Supabase Edge Function

Проверка пароля на бэкенде:

```typescript
// Edge Function
export const validateAdminPassword = async (password: string) => {
  const correctHash = 'hash-of-password'; // bcrypt hash
  return bcrypt.compare(password, correctHash);
};
```

**Преимущества:**
- ✅ Пароль не в клиентском коде
- ✅ Можно логировать попытки
- ✅ Максимальная безопасность

**Недостатки:**
- ⚠️ Сложнее в реализации
- ⚠️ Требует backend

### Вариант 3: Только роль в БД (без пароля)

Полагаться только на `user_roles`:

**Преимущества:**
- ✅ Простота
- ✅ Нет дополнительного пароля

**Недостатки:**
- ⚠️ Меньше безопасность
- ⚠️ Если БД скомпрометирована - полный доступ

---

## ✅ Итог

**Текущая реализация:**
- 🔐 Двухфакторная защита (роль + пароль)
- 🛡️ Защита от брутфорса (3 попытки)
- ⏰ Временные сессии (8 часов)
- 🚫 Блокировка при неудачных попытках
- 💾 sessionStorage (не persistent)

**Баланс безопасности и удобства:** ⭐⭐⭐⭐☆

**Рекомендация:**
- ✅ Для малых/средних проектов - **идеально**
- ✅ Для production - добавить HTTPS
- ⚠️ Для enterprise - использовать Edge Function

---

**Документация:** ADMIN_PASSWORD_SETUP.md  
**Версия:** 1.0.0  
**Дата:** 11 ноября 2025

Храните пароль в безопасности! 🔒
